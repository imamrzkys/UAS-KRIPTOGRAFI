/**
 * NeobrutalBackground — decorative pastel dots/dashes pattern
 * Replaces the dark canvas particle system.
 */
export default function AnimatedBackground() {
  // In the neobrutalism theme we use a simple CSS dot grid
  // rendered via an SVG background on the page body — nothing rendered here.
  return null;
}
